import { toggleDropdown, toggleMenu } from "./MenuFunctions";
import { currentSlide, plusSlides } from "./SliderFunctions";

export const toggleMenuUtils = toggleMenu;
export const toggleDropdownUtils = toggleDropdown;
export const currentSlideUtils = currentSlide;
export const plusSlidesUtils = plusSlides;
